Kattegat ship-scenario reference baseline — PROVENANCE
=======================================================

Date generated:   2026-06-04
Producing code:   branch ship-deterrence-port, commit d3b945d
                  (DEPONS ship-deterrence vectorized port; see
                   docs/superpowers/plans/2026-06-04-ship-deterrence-vectorized-port.md
                   and docs/superpowers/specs/2026-06-03-ship-deterrence-parity-investigation.md)
Command:          python3 scripts/run_kattegat_reference.py --count 2000 --years 2 --seed 42 --ships
Landscape:        data/Kattegat (600x1000 cells), 637 real ship routes from data/Kattegat/ships.json
Seed:             42 (deterministic; DEPONS parameters.xml randomSeed is __NULL__)

SUPERSEDES the prior baseline (commit 27824e1), which was generated BEFORE the
ship-deterrence fix and showed deter_strength == 0 for the entire run because the
production vectorized path applied the turbine deterrence model to ships.

This baseline reflects LIVE DEPONS ship deterrence (Tships=80 dB gate, 10 km cap,
probabilistic pship_*/cship_* response, loudest-ship-wins, L2 deter_strength):
  - 2 yr (34,560 ticks), N=2000, population stable (2000 -> 1998)
  - 38,093 nonzero deter_strength records, max 0.0409
    (the ~0.04 magnitude is < the 0.1 UI is_disturbed flag but > the 0.01
     disturbance-memory threshold — see the threshold-audit note in the plan)

Committed files are the compact DEPONS-format outputs (Population.txt, Energy.txt,
Mortality.txt) plus this PROVENANCE.txt. The heavy per-individual / per-event files
(PorpoiseStatistics.txt, Dispersal.txt) are gitignored and regenerable via the
command above.
